# addPrefetchLink
Adds links with `rel="prefetch"` to your `<head>`-element to speed up navigation

## Limitations
This package is build with the latest module standard 
to date so it won't work as an AMD or CommonJS module